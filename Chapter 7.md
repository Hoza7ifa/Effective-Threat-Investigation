### شرح مفصل وبسيط للشابتر السابع من كتاب "Effective Threat Investigation for SOC Analysts" لمؤلفه مصطفى يحيى

الشابتر السابع بعنوان **"Investigating Persistence and Lateral Movement Using Windows Event Logs"** يركز على تعليم محللي مراكز العمليات الأمنية (SOC Analysts) ومستجيبي الحوادث كيفية التعرف على تقنيات **الاستمرارية (Persistence)** و**الحركة الجانبية (Lateral Movement)** التي يستخدمها المهاجمون في الشبكات المخترقة، وكيفية التحقيق في هذه الأنشطة باستخدام **سجلات أحداث ويندوز (Windows Event Logs)**. الهدف هو تمكين المحلل من اكتشاف هذه التقنيات وفهمها وتحليلها لمواجهة الهجمات السيبرانية بفعالية.

---

### **نظرة عامة على الشابتر**

الشابتر مقسم إلى قسمين رئيسيين:

1. **تقنيات الاستمرارية (Persistence Techniques)**: كيف يحافظ المهاجمون على وجودهم في النظام المخترق حتى بعد إعادة تشغيل النظام أو تغيير بيانات الاعتماد.
2. **تقنيات الحركة الجانبية (Lateral Movement Techniques)**: كيف ينتقل المهاجمون من جهاز مخترق إلى أجهزة أخرى في الشبكة للوصول إلى بيانات حساسة أو أنظمة ذات قيمة عالية.

كل قسم يشرح التقنيات المستخدمة، وكيف يمكن اكتشافها من خلال تحليل سجلات الأحداث، مع أمثلة عملية من أدوات مثل **HELK** وبيانات أمنية مثل **APT29** و**Mordor Security Datasets**.

---

### **القسم الأول: تقنيات الاستمرارية (Persistence Techniques)**

الاستمرارية هي الطرق التي يستخدمها المهاجمون لضمان استمرار الوصول إلى النظام المخترق حتى بعد تغييرات مثل إعادة التشغيل أو تسجيل الخروج أو تغيير كلمات المرور. الشابتر يركز على أربع تقنيات رئيسية للاستمرارية وكيفية اكتشافها باستخدام سجلات أحداث ويندوز.

#### **1. مفاتيح التسجيل (Registry Run Keys)**

**ما هي؟**

- المهاجمون يضيفون مسارات لبرمجياتهم الخبيثة إلى مفاتيح التسجيل (Registry Keys) في ويندوز، مثل:
    - **HKEY_LOCAL_MACHINE\Software\Microsoft\Windows\CurrentVersion\Run**
    - **HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run**
- هذه المفاتيح تجعل البرمجيات الخبيثة تعمل تلقائيًا عند تسجيل دخول المستخدم أو تشغيل النظام.

**كيف تكتشفها؟**

- **Event ID 4657**: يسجل تعديل قيمة في التسجيل (Registry Value Modified). هذا الحدث يوفر تفاصيل عن القيمة الجديدة أو المعدلة، مما يساعد في تحديد البرمجيات الخبيثة.
- **Event ID (غير محدد في النص)**: يسجل الوصول إلى مفاتيح التسجيل (مثل عندما يصل PowerShell.exe إلى مفتاح Run).
- **مثال عملي**: إذا رأيت عملية PowerShell.exe تصل إلى مفتاح Run بشكل غير طبيعي (مثل خارج ساعات العمل)، قد يكون ذلك مؤشرًا على نشاط خبيث.

**نصائح للمحللين**:

- ابحث عن الوصول إلى مفاتيح التسجيل في أوقات غير معتادة.
- تحقق من مسارات الملفات في القيم المضافة، خاصة إذا كانت تشير إلى مواقع غير موثوقة مثل مجلدات المستخدم أو المجلدات المؤقتة.

#### **2. المهام المجدولة (Windows Scheduled Tasks)**

**ما هي؟**

- المهاجمون ينشئون مهام مجدولة (Scheduled Tasks) لتشغيل برمجياتهم الخبيثة بشكل دوري أو عند تلبية شروط معينة (مثل تسجيل دخول المستخدم).
- يمكن إنشاء هذه المهام باستخدام أدوات مثل **schtasks.exe** أو واجهة المستخدم الرسومية.

**مثال عملي**:

- مجموعة **APT3** استخدمت الأمر التالي لإنشاء مهمة مجدولة:
    
    text
    
    طيالتفاف
    
    نسخ
    
    `   schtasks /create /tn mytask /tr C:\Users\Public\test.exe /sc ONLOGON /ru System       `
    
    هذا الأمر ينشئ مهمة تُسمى "mytask" لتشغيل ملف خبيث عند تسجيل دخول المستخدم باستخدام حساب النظام (System).

**كيف تكتشفها؟**

- **Event ID 4698**: يسجل إنشاء مهمة مجدولة جديدة في سجل الأمان (Security Event Log). يوفر تفاصيل مثل اسم المهمة، المسار، والحساب المستخدم.
- **مثال عملي**: في 2 سبتمبر 2020، الساعة 3:15 صباحًا، تم إنشاء مهمة تُسمى "MordorScotl" لتشغيل نص PowerShell يوميًا الساعة 9:10 صباحًا باستخدام حساب "pguartavo".

**نصائح للمحللين**:

- ابحث عن مهام مجدولة تم إنشاؤها خارج ساعات العمل العادية.
- تحقق من مسارات الملفات المشبوهة (مثل الملفات في مجلدات المستخدم أو Temp).
- راقب استخدام أدوات مثل PowerShell أو CMD في المهام المجدولة.

#### **3. الخدمات (Windows Services)**

**ما هي؟**

- المهاجمون ينشئون خدمات ويندوز جديدة لتشغيل برمجياتهم الخبيثة تلقائيًا عند بدء تشغيل النظام.
- الخدمات يمكن أن تُدار باستخدام أوامر مثل **sc.exe** أو واجهات برمجية.

**مثال عملي**:

- تم إنشاء خدمة تُسمى "Java(TM) Virtual Machine Support Service" لتشغيل ملف **javantsup.exe** من مجلد **System32** بحساب **LocalSystem**. هذا مشبوه لأن **javantsup.exe** ليس ملفًا افتراضيًا في ويندوز.

**كيف تكتشفها؟**

- **Event ID 4697**: يسجل إنشاء خدمة جديدة في سجل الأمان. يوفر تفاصيل مثل اسم الخدمة، المسار، نوع البدء (مثل Auto Start)، وحساب التشغيل.
- **Event ID 7045**: يسجل إنشاء خدمة جديدة في سجل النظام (System Event Log)، ويتضمن نفس التفاصيل مثل Event ID 4697.
- **مثال عملي**: إذا رأيت خدمة باسم غير مألوف أو مسار ملف غير قياسي (مثل **C:\Windows\System32\javantsup.exe**)، فهذا مؤشر على نشاط خبيث.

**نصائح للمحللين**:

- ركز على الخدمات التي تستخدم نوع بدء "Auto Start" لأنها شائعة في تقنيات الاستمرارية.
- تحقق من مسارات الملفات وتأكد من أنها ملفات ويندوز قياسية.
- راقب الخدمات التي تعمل بحسابات مرتفعة الصلاحيات مثل **LocalSystem**.

#### **4. اشتراكات أحداث WMI (WMI Event Subscription)**

**ما هي؟**

- المهاجمون يستخدمون **Windows Management Instrumentation (WMI)** لإنشاء اشتراكات أحداث تُنفَّذ عند تحقق شروط معينة (مثل كل دقيقة).
- تتطلب ثلاث خطوات:
    1. إنشاء **Event Filter** لتحديد الشرط (مثل الوقت).
    2. إنشاء **Event Consumer** لتحديد الأمر أو النص البرمجي الذي سيُنفَّذ.
    3. إنشاء **Binding** لربط الفلتر بالمستهلك.

**مثال عملي**:

- تم إنشاء مستهلك WMI باسم "Updater" مرتبط بفلتر يُسمى "Updater"، وهو ينفذ أمر PowerShell مشفر بشكل مشبوه.

**كيف تكتشفها؟**

- **Event ID 5861**: يسجل إنشاء مستهلك WMI جديد في سجل **Microsoft-Windows-WMI-Activity/Operational**. يوفر تفاصيل مثل اسم المستهلك، نوعه (مثل **CommandLineEventConsumer** أو **ActiveScriptEventConsumer**)، والأوامر المرتبطة.
- **مثال عملي**: إذا رأيت مستهلك WMI ينفذ أوامر PowerShell مشفرة أو يشير إلى مسارات مشبوهة، فهذا مؤشر على نشاط خبيث.

**نصائح للمحللين**:

- تحقق من أنواع المستهلكات (CommandLineEventConsumer وActiveScriptEventConsumer) لأنها شائعة في الهجمات.
- ابحث عن أسماء فلاتر أو مستهلكات غير مألوفة.
- راقب الأوامر أو النصوص التي تُنفَّذ، خاصة إذا كانت تشير إلى ملفات في مواقع غير موثوقة.

---

### **القسم الثاني: تقنيات الحركة الجانبية (Lateral Movement Techniques)**

الحركة الجانبية هي العملية التي ينتقل فيها المهاجمون من جهاز مخترق إلى أجهزة أخرى في الشبكة للوصول إلى بيانات حساسة أو أنظمة ذات قيمة عالية. الشابتر يغطي أربع تقنيات رئيسية وكيفية اكتشافها باستخدام سجلات الأحداث.

#### **1. الاتصال بسطح المكتب البعيد (Remote Desktop Connection)**

**ما هي؟**

- المهاجمون يستخدمون أداة **Remote Desktop Protocol (RDP)** المدمجة في ويندوز للوصول إلى أنظمة أخرى والتحكم بها.
- RDP يُعتبر حركة مرور شرعية، مما يجعل اكتشافه صعبًا.

**كيف تكتشفها؟**

- **سجلات الجهاز المصدر (Source Machine)**:
    - **Event ID 4688**: يسجل تنفيذ عملية **mstsc.exe** (عميل RDP).
- **سجلات الجهاز الهدف (Target Machine)**:
    - **Event ID 4688**: يسجل تنفيذ عمليات مثل **rdpclip.exe** و**tscon.exe**.
    - **Event ID 4624**: يسجل تسجيل دخول ناجح عبر RDP (Logon Type 10). يوفر اسم المستخدم، النطاق، وعنوان IP المصدر.
    - **Event ID 4778**: يسجل إعادة الاتصال بجلسة RDP. يوفر اسم الجهاز المصدر وعنوان IP.
    - **Event ID 4779**: يسجل قطع الاتصال بجلسة RDP.
- **مثال عملي**: حساب **phesely** سجل دخوله إلى جهاز **SCRANTON** من جهاز **CAT** (IP: 172.18.39.2).

**نصائح للمحللين**:

- تحقق مما إذا كان الاتصال RDP بين محطتين عاديتين (Client-to-Client) بدلاً من محطة إدارية.
- راقب الاتصالات خارج ساعات العمل أو من أجهزة غير مصرح لها.
- تأكد من أن اسم الجهاز المصدر في **Event ID 4624** يشير إلى الجهاز الهدف وليس المصدر (قد يكون مربكًا).

#### **2. مشاركات الإدارة (Windows Admin Shares)**

**ما هي؟**

- المهاجمون يستخدمون حسابات بصلاحيات إدارية للوصول إلى مشاركات إدارية مثل **C$**، **ADMIN$**، أو **IPC$** عبر بروتوكول SMB.
- يستخدمون أدوات مثل **net.exe** لربط المشاركات (مثل: net use \\targetip\C$ /user:domain\user password).

**كيف تكتشفها؟**

- **سجلات الجهاز المصدر**:
    - **Event ID 4688**: يسجل تنفيذ عمليات **net.exe** أو **net1.exe**، مع تفاصيل الأوامر التي تكشف عن الهدف.
- **سجلات الجهاز الهدف**:
    - **Event ID 4624**: يسجل تسجيل دخول ناجح (Logon Type 3) للوصول إلى المشاركات.
    - **Event ID 4672**: يسجل تخصيص امتيازات إدارية، ويظهر بعد Event ID 4624.
    - **Event ID 5140**: يسجل الوصول إلى مجلدات مشتركة.
    - **Event ID 5145**: يسجل فحص الوصول إلى كائن مشترك.
- **مثال عملي**: حساب **ppeesly** وصل إلى مشاركة شبكية من عنوان IP **10.0.1.4**.

**نصائح للمحللين**:

- تحقق من نوع تسجيل الدخول (Logon Type 3) في **Event ID 4624**.
- راقب استخدام **net.exe** مع أوامر تشير إلى عناوين IP أو أسماء مضيفين مشبوهة.
- تأكد من أن الحسابات المستخدمة لها صلاحيات إدارية.

#### **3. أداة PsExec (Sysinternals PsExec)**

**ما هي؟**

- **PsExec** هي أداة من Sysinternals تُستخدم لتنفيذ الأوامر عن بُعد. المهاجمون يستخدمونها لنقل وتنفيذ ملفات خبيثة على أنظمة أخرى.
- مثال: psexec.exe \\HostB -accepteula -d c:\MalwareFolder\Malware.exe ينقل وينفذ ملف **Malware.exe** على **HostB**.

**كيف تكتشفها؟**

- **سجلات الجهاز المصدر**:
    - **Event ID 4688**: يسجل تنفيذ **psexec.exe** مع تفاصيل الأوامر.
- **سجلات الجهاز الهدف**:
    - **Event ID 4624**: يسجل تسجيل دخول ناجح (Logon Type 3) للوصول إلى مشاركة **ADMIN$**.
    - **Event ID 4697**: يسجل إنشاء خدمة جديدة تُسمى **PSEXESVC**.
    - **Event ID 4688**: يسجل تنفيذ **PSEXESVC.exe** بواسطة **services.exe**، وأي عمليات فرعية (مثل **Malware.exe**).
- **مثال عملي**: تم إنشاء خدمة **PSEXESVC** لتشغيل **PSEXESVC.exe** من **C:\Windows**، ثم تم تنفيذ ملف خبيث من مجلد **C:\Windows\Temp**.

**نصائح للمحللين**:

- راقب إنشاء خدمات **PSEXESVC** أو تنفيذ ملفات من مسارات مؤقتة.
- تتبع العمليات الفرعية التي تُنشأ بواسطة **PSEXESVC.exe** باستخدام **Event ID 4688**.
- تحقق من الأوامر في **Event ID 4688** لمعرفة الهدف.

#### **4. PowerShell Remoting**

**ما هي؟**

- المهاجمون يستخدمون **PowerShell Remoting** عبر بروتوكول **WinRM** لتنفيذ أوامر على أنظمة أخرى عن بُعد.
- أوامر شائعة: Invoke-Command أو Enter-PSSession.

**كيف تكتشفها؟**

- **سجلات الجهاز المصدر**:
    - **Event ID 4688**: يسجل تنفيذ **powershell.exe**.
    - **Event ID 4104** و**Event ID 800**: يسجلان الأوامر والنصوص المنفذة في PowerShell.
- **سجلات الجهاز الهدف**:
    - **Event ID 4624**: يسجل تسجيل دخول ناجح (Logon Type 3).
    - **Event ID 4688**: يسجل تنفيذ **wsmprovhost.exe**، وهي العملية التي تتعامل مع جلسات PowerShell البعيدة.
- **مثال عملي**: تم تنفيذ **wsmprovhost.exe** على الجهاز الهدف لاستقبال أوامر PowerShell من الجهاز المصدر.

**نصائح للمحللين**:

- راقب تنفيذ **wsmprovhost.exe** على الجهاز الهدف.
- تحقق من الأوامر المسجلة في **Event ID 4104** و**800** للكشف عن أوامر مشبوهة.
- تأكد من أن الاتصالات عبر WinRM ليست من مصادر غير مصرح بها.

---

### **أهم النقاط لمحلل SOC ناجح**

لكي تكون محلل SOC ناجح، تحتاج إلى التركيز على النقاط التالية من الشابتر:

1. **فهم سجلات الأحداث (Windows Event Logs)**:
    - تعلم معرفات الأحداث الرئيسية (مثل 4657، 4698، 4697، 5861، 4624، 4688، 4778، 4779، 5140، 5145) وما تعنيه.
    - افهم الفروق بين سجلات الأمان (Security)، النظام (System)، وWMI (Microsoft-Windows-WMI-Activity/Operational).
2. **الكشف عن الأنشطة المشبوهة**:
    - ابحث عن الأنشطة خارج ساعات العمل العادية.
    - تحقق من مسارات الملفات غير الموثوقة (مثل Temp أو مجلدات المستخدم).
    - راقب استخدام أدوات "Living off the Land" مثل PowerShell، CMD، أو net.exe.
3. **تمييز الأنشطة الشرعية عن الخبيثة**:
    - تحقق مما إذا كانت الأنشطة (مثل RDP أو PowerShell) تأتي من أجهزة إدارية أو عادية.
    - استخدم سياق الحدث (مثل نوع تسجيل الدخول أو عنوان IP المصدر) لتحديد ما إذا كان النشاط شرعيًا.
4. **استخدام أدوات التحليل**:
    - تعلم استخدام أدوات مثل **HELK** لتحليل السجلات.
    - استفد من قواعد البيانات الأمنية مثل **Mordor Security Datasets** لفهم سيناريوهات الهجمات.
5. **التركيز على التفاصيل**:
    - تتبع العمليات الفرعية (مثل العمليات التي تُنشأ بواسطة **PSEXESVC.exe** أو **wsmprovhost.exe**).
    - افحص أوامر PowerShell المشفرة أو الملفات في مسارات غير قياسية.
6. **البحث الاستباقي (Threat Hunting)**:
    - قم بفحص سجلات الأحداث بانتظام للكشف عن أنماط مشبوهة قبل حدوث خرق.
    - استخدم معرفات الأحداث لتحديد تقنيات الاستمرارية والحركة الجانبية.

---

### **الخلاصة**

الشابتر السابع يقدم دليلًا شاملًا لفهم تقنيات الاستمرارية (مفاتيح التسجيل، المهام المجدولة، الخدمات، وWMI) والحركة الجانبية (RDP، مشاركات الإدارة، PsExec، وPowerShell Remoting). من خلال تحليل سجلات أحداث ويندوز، يمكن لمحللي SOC اكتشاف هذه الأنشطة والاستجابة لها بفعالية. التركيز على معرفات الأحداث الرئيسية، فهم سياق الأنشطة، واستخدام أدوات التحليل هو المفتاح لتكون محلل SOC ناجح.